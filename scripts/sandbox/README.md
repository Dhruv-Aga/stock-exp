# Custom scripts for the trading assistant

Place `.py` or `.js` files here. The assistant can run them via:
- `run_python_script` tool
- `run_node_script` tool

Example: `list_tickers.py` prints configured markets.
